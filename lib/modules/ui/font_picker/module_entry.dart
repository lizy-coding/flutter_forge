import 'package:flutter/material.dart';

import 'module_root.dart';

class FontPickerEntry extends StatelessWidget {
  const FontPickerEntry({super.key});

  @override
  Widget build(BuildContext context) => const HomePage();
}
