# macOS current window timing rerun

Fresh Release from the current workspace created the asynchronous category window with a visible first frame. Closing the category window no longer terminated the process. The main window was recoverable through the macOS Window menu and its full accessibility tree was readable.

Three close/reopen rounds and adsorption_line visual regression remain to be completed before changing the gate to PASS. Windows self-test remains blocked.
