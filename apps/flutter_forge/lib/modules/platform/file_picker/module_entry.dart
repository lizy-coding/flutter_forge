import 'package:flutter/material.dart';

import 'module_root.dart';

class FilePickerEntry extends StatelessWidget {
  const FilePickerEntry({super.key});

  @override
  Widget build(BuildContext context) => const HomePage();
}
