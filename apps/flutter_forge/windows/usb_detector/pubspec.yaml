name: usb_detector_windows
description: Windows SetupAPI USB detection plugin for Flutter Forge.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.5
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      windows:
        pluginClass: UsbDetectorWindowsPlugin
