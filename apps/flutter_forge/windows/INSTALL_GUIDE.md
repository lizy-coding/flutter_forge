# Flutter Forge Windows 安装说明

此预览版安装器未使用 Authenticode 代码签名，Windows SmartScreen 可能在首次运行时显示保护提示。

1. 运行 `flutter_forge-setup-x64.exe`。
2. 如果 SmartScreen 出现提示，点击“更多信息（More info）”。
3. 点击“仍要运行（Run anyway）”，然后按安装向导完成安装。

不希望安装时，也可以解压 `flutter_forge-windows-x64.zip` 并直接运行其中的 `flutter_forge.exe`。
