import 'package:flutter/material.dart';

import 'pages/font_picker_page.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) => const FontPickerPage();
}
